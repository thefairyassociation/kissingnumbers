# Exact joint-layer search trigger

This branch exists to execute the exact 4,248-direction search for a 169-point ZE99 half-height layer as a pull-request check against `codex/d13-joint-layer-169`.

The search is fail-closed: a dimension-13 record is emitted only after full exact `Q(sqrt(3))` verification of at least 1,155 vectors.
