# One-tetrad-deletion search trigger

This branch executes six exact searches, one representative from each deletion type of the 51 ZE99 tetrad supports. Each search asks whether removing 16 equatorial vectors permits a 177-point half-height layer, which would produce an exact 1,156-vector dimension-13 kissing configuration.
